# Dev overrides: hot-reload volume mount
services:
  web:
    build:
      target: development
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    command: ["pnpm", "dev"]
    environment:
      NODE_ENV: development
